<h1>Create a New Page </h1>

<%= form_for(@static_page) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :permalink %>
  <%= f.text_field :permalink %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.submit 'Create Page' %>

<% end %>
